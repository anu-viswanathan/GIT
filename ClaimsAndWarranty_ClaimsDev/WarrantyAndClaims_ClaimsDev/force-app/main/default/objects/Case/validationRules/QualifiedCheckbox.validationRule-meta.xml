<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>QualifiedCheckbox</fullName>
    <active>true</active>
    <description>Qualifed checkbox can be checked only when there’s a valid product entered and the primary claim type is one of the organics and secondary is either 05 or 44.</description>
    <errorConditionFormula>AND(
    OR(
        RecordType.Name = &apos;Commercial Claim&apos;,
        RecordType.Name = &apos;Residential Claim&apos; 
    ),
    Qualified__c = TRUE,
    OR(
        Total_Organic_Type__c &lt; 1,
        ISBLANK(Product__c)  
    )
    ,
    $Profile.Name &lt;&gt; &apos;System Administrator&apos;
)</errorConditionFormula>
    <errorMessage>In order to select the Qualified option, a product must be entered and the primary claim type must be organic while the Concern is Granule Loss or Cracking.</errorMessage>
</ValidationRule>
