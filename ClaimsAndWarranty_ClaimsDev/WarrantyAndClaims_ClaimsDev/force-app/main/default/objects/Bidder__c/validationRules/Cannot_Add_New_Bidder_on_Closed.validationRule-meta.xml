<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cannot_Add_New_Bidder_on_Closed</fullName>
    <active>true</active>
    <errorConditionFormula>AND( 
    ISNEW(),
    OR(  
       ISPICKVAL(Opportunity__r.StageName, &apos;Closed Lost&apos;),
       ISPICKVAL(Opportunity__r.StageName, &apos;Closed Won&apos;),
       ISPICKVAL(Opportunity__r.StageName, &apos;Closed Expired&apos;),
       ISPICKVAL(Opportunity__r.StageName, &apos;Closed Rejected&apos;)
    )
)</errorConditionFormula>
    <errorMessage>Cannot Add new Bidder if Opportunity is Closed</errorMessage>
</ValidationRule>
