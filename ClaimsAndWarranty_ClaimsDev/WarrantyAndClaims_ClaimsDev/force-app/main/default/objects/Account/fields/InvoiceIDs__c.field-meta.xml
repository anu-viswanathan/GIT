<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>InvoiceIDs__c</fullName>
    <description>Field to store last uploaded invoice ID, so ID can be stored in text area, used to check individually uploaded invoices (via flow - to determine if an invoice should be calculated with the totals or not depending on the invoice period chosen in custom settings)</description>
    <externalId>false</externalId>
    <label>InvoiceIDs</label>
    <length>255</length>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
