<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Contractor_IAAP_Registration_Valid__c</fullName>
    <description>Binary flag to indicate if the IAAP registration is valid. At the moment this is purely based on the expiry date--we use this on related records to display page based error messages which for some reason work with checkboxes but not with date fields!.</description>
    <externalId>false</externalId>
    <formula>IF( IAP_Registration_Expiry_Date__c &gt;= TODAY(), true, false)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Contractor IAAP Registration Valid?</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
