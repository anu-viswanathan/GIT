<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Parent_Name_Address_Verified__c</fullName>
    <description>/* If the account has no partent, mark this as true (nothing to verify. Otherwise, the value from the parent.) */</description>
    <externalId>false</externalId>
    <formula>IF(ISBLANK( ParentId ), true,
 Parent.Account_Name_Address_Verified__c )

/* If the account has no partent, mark this as true (nothing to verify. Otherwise, the value from the parent.) */</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>If the account has a parent, has it&apos;s address and name been verified?</inlineHelpText>
    <label>Parent Name &amp; Address Verified</label>
    <trackHistory>false</trackHistory>
    <type>Checkbox</type>
</CustomField>
