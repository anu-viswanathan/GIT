<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Simplified_Signup_Account_Payment__c</fullName>
    <description>Ticked when it concerns a contractor (applicant) that goes through the simplified one step signup process (in this case the account payment is linked to the account instead of to the applicant contact record).</description>
    <externalId>false</externalId>
    <formula>IF(NOT(ISBLANK(Applicant__c)), Applicant__r.Simplified_Signup__c, 

IF(NOT(ISBLANK(Contractor__c)), Contractor__r.Applicant_Information__r.Simplified_Signup__c , FALSE)

)</formula>
    <label>Simplified Signup</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
