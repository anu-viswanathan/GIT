<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Report_Due_Date__c</fullName>
    <externalId>false</externalId>
    <formula>IF( Today()&gt;Due_Date__c, 1,
IF( AND(Due_Date__c&gt;=Today(), Due_Date__c - Today() &lt; 31), 30,
IF(AND(Due_Date__c - Today()&gt;30, 
       Due_Date__c - Today()&lt;61
       ),60, 
IF(AND(Due_Date__c - Today()&gt;60,
	Due_Date__c - Today()&lt;91
       ),90,
	91
  )
)
)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Report_Due Date</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
