<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Applicant__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Lookup to contractor applicant contact in order to store the payment record for the signup process payment. (payment info will be copied to the contractor account once that account is created).</description>
    <externalId>false</externalId>
    <inlineHelpText>Lookup to contractor applicant contact in order to store the payment record for the signup process payment. (payment info will be copied to the contractor account once that account is created).</inlineHelpText>
    <label>Applicant</label>
    <referenceTo>Contact</referenceTo>
    <relationshipName>Account_Payments</relationshipName>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
