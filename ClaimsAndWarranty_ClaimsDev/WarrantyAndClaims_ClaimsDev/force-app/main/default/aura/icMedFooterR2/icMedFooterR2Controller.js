/**
 * Created by yvesasselin on 2018-08-12.
 */
({
    doInit : function (component, event, helper) {
        helper.doInit(component, event);
    },

    navToPrivacyPolicy : function (component, event, helper) {
        helper.navToPrivacyPolicy(component);
    },

    navToCookiePolicy : function (component, event, helper) {
        helper.navToCookiePolicy(component);
    },

    navToTermsConditions : function (component, event, helper) {
        helper.navToTermsConditions(component);
    },

    navToGlobalSite : function (component, event, helper) {
        helper.navToGlobalSite(component);
    },

    navToNorthAmericanSite : function (component, event, helper) {
        helper.navToNorthAmericanSite(component);
    },

    navToContactUs : function (component, event, helper) {
        helper.navToContactUs(component);
    },


})