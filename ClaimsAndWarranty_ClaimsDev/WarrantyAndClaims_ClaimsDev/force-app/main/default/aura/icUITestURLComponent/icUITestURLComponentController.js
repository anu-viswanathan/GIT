({
	doInit : function (component, event, helper) {
		helper.doInit(component, event);
	},
    
    navToHomeRelativeURL : function (component, event, helper) {
        helper.navToHomeRelativeURL(component);
    },

    navToHomeDev2 : function (component, event, helper) {
        helper.navToHomeDev2(component);
    },

    navToProzone : function (component, event, helper) {
        helper.navToProzone(component);
    },

    navToComingSoon : function (component, event, helper) {
        helper.navToComingSoon(component);
    },

    navToTestAppPage : function (component, event, helper) {
        helper.navToTestAppPage(component);
    },


    navToMyToolbox : function (component, event, helper) {
        helper.navToMyToolbox(component);
    }

})