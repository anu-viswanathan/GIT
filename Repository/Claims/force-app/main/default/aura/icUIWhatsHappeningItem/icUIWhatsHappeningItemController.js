({
    navToInternal : function (component, event, helper) {
        helper.navToInternal(component);
    },

    navToExternal : function (component, event, helper) {
        helper.navToExternal(component);
    },

    toggleText : function (component, event, helper) {
        helper.toggleText(component);
    } 
})