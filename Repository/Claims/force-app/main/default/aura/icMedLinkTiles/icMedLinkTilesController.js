({
    doInit : function (component, event, helper) {
        helper.doInit(component, event);
    },

    navToMyDashboard : function (component, event, helper) {
        helper.navToMyDashboard(component);
    },
    navToMyLeads : function (component, event, helper) {
        helper.navToMyLeads(component);
    },
    navToMyRewards : function (component, event, helper) {
        helper.navToMyRewards(component);
    },
    navToIkoUniversity : function (component, event, helper) {
        helper.navToIkoUniversity(component);
    },

    navToProzone : function (component, event, helper) {
        helper.navToProzone(component);
    },

    navToForum: function (component, event, helper) {
        helper.navToForum(component);
    },






})